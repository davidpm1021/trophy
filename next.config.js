/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/trophy',
  assetPrefix: '/trophy/',
}

module.exports = nextConfig 